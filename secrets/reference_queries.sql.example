-- Reference (naive baseline) queries for Q5–Q9.
--
-- Copy this file to `secrets/reference_queries.sql` ON THE SERVER ONLY and fill
-- in the real queries. The real file is gitignored and must NEVER be committed:
-- it is the source of the golden answers (see the tech design § "Golden results
-- & reference queries"). At setup, a loader runs each query against the seeded
-- DB and stores the normalised result in `app.questions.golden_result` — the DB
-- volume is the only place the answer ever lives.

-- Q5: products per category
-- SELECT ...;

-- Q6: top customers by total spending (Σ orders.total_amount per customer)
-- SELECT ...;

-- Q7: 1000 most recent orders + customer info
-- SELECT ...;

-- Q8: low-stock products (stock_quantity < 10)
-- SELECT ...;

-- Q9: revenue per category (Σ quantity × unit_price via order_details)
-- SELECT ...;
