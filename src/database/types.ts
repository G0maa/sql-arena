/**
 * Kysely database schema.
 *
 * Tables are added in Step 2 (DB bootstrap) — the `seed` schema (the e-commerce
 * dataset under study) and the `app` schema (questions / submissions queue /
 * leaderboard). For the Step 1 scaffold this is intentionally empty: it exists
 * so the Kysely instance is typed and the connection can be verified.
 */
export interface Database {}
